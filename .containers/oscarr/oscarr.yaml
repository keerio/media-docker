services:
  # Oscarr
  oscarr:
    container_name: oscarr
    volumes:
      - ${CONTAINER_DIR}/oscarr:/data
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TIMEZONE}
    networks:
      - proxied
      - internal
    restart: unless-stopped
