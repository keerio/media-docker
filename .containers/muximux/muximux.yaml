services:
  # Muximux
  muximux:
    container_name: muximux
    volumes:
      - ${CONTAINER_DIR}/muximux/${CONTAIN_CONF_SUBDIR}:/config
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TIMEZONE}
    networks:
      - internal
      - proxied
    restart: unless-stopped
