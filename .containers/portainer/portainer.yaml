services:
  # Portainer
  portainer:
    container_name: portainer
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${CONTAINER_DIR}/portainer//${CONTAIN_CONF_SUBDIR}:/data
    networks:
      - internal
      - proxied
    restart: unless-stopped
