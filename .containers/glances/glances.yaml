services:
  glances:
    container_name: glances
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${CONTAINER_DIR}/glances:/glances
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TIMEZONE}
      - GLANCES_OPT=-w
    networks:
      - internal
      - proxied
    pid: host
    restart: unless-stopped
