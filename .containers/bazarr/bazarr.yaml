services:
  # Bazarr
  bazarr:
    container_name: bazarr
    volumes:
      - ${CONTAINER_DIR}/bazarr/${CONTAIN_CONF_SUBDIR}:/config
      - ${MEDIA_DIR_MOVIES}:/movies
      - ${MEDIA_DIR_TV}:/tv
      - ${DOWNLOAD_DIR}:/downloads
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TIMEZONE}
    networks:
      - proxied
      - internal
    restart: unless-stopped
