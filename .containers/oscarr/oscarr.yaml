services:
  # Oscarr
  oscarr:
    container_name: oscarr
    volumes:
      - ${BASE_DIR}/oscarr/config:/oscarr/data
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TIMEZONE}
    networks:
      - proxied
      - internal
    restart: always
