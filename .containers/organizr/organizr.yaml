services:
  # Organizr
  organizr:
    container_name: organizr
    volumes:
      - ${CONTAINER_DIR}/organizr/${CONTAIN_CONF_SUBDIR}:/config
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TIMEZONE}
    networks:
      - internal
      - proxied
    restart: unless-stopped
